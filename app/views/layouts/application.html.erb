    <!DOCTYPE html>
<html>
  <head>
    <title>FREE_YOGA</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <nav class="navbar navbar-default">
        <!--ここから下がハンバーガーメニューの描画↓↓-->
        <button type="button" class="navbar-toggle collapsed"   
        data-toggle="collapse" data-target="#gnavi">
          <span class="sr-only">Bootstrap</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>  
        <!--ここから上がハンバーガーメニューの描画-->
        <a class="navbar-brand" href="/">HOME</a> <!--この部分が左側にあるロゴの部分-->
        
        
        <div id="gnavi" class="collapse navbar-collapse"> <!--ここからがメニューリストの部分-->
          <ul class="nav navbar-nav">
            <li class="col-sm"><%= link_to 'イベント一覧', events_path %></li>
            
            <li class="col-sm"><%= link_to "お問い合わせ", new_contact_path %></li>
            
          <div class="app-header-inner">
            <% if logged_in? %>
              <li class="col-sm"><%= link_to '新しく投稿する', new_event_path %></li>
              <div class="col-sm"><%= link_to "Profile", user_path(current_user.id) %>
              <%= link_to "Logout", user_session_path(current_user.id), method: :delete %></div>
            <% else %>
              <div class="col-sm"><%= link_to "ヨガインストラクターの登録", new_user_path %>
                <%= link_to "Login", new_user_session_path %></div>
            <% end %>
          </div>
          </ul>
        </div>
      </nav>
      
    
      <div class="clearfix:after">  
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %>">
            <%= value %>
          </div>
        <% end %>
        <%= yield %>  
      </div>  
    
    
    <footer class="footer">
      <div class="container">
        <p class="text-muted">Copyright &copy; FreeYoga</p>
      </div> 
    </footer>
  </body>
</html>

    
