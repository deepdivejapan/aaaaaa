<h1>Log in</h1>
  <%= form_with(scope: :user_session, url: user_sessions_path, local: true) do |f| %>

    <%= f.label :email %>
    <%= f.email_field :email, size: "35" %>
  
    <%= f.label :password %>
    <%= f.password_field :password, size: "35" %>
  
    <%= f.submit "Log in" %>
  <% end %>
